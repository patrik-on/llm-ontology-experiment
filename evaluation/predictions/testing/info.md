# evaluation/predictions/testing

Predikcie modelov pre JUnit test generation test split.

Každý JSONL súbor zodpovedá jednému modelu. Súbory vznikajú cez `scripts/run_inference_eval.py` alebo `scripts/run_full_evaluation.py`.
