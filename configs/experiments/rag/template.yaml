experiment:
  name: "rag_qwen25_coder_7b"
  approach: "rag"
  enabled: false

model:
  config: "configs/models/qwen25_coder_7b_hf_wsl.yaml"

retrieval:
  mode: "single_collection_rag"
  allowed_splits:
    - "train"
  collection: "test_examples"
  metadata_filter: {}
  top_k: 3
  max_context_tokens: 2048

notes:
  - "Phase-1 indexing and retrieval are implemented; experiment-runner wiring remains disabled."
  - "Replace deterministic_mock embeddings before reporting research results."
