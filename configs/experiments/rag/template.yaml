experiment:
  name: "rag_qwen25_coder_7b"
  approach: "rag"
  enabled: false

model:
  config: "configs/shared/models/qwen25_coder_7b_ollama.yaml"

retrieval:
  mode: "single_collection_rag"
  allowed_splits:
    - "train"
  collection: "mixed"
  metadata_filter: {}
  top_k: 5
  max_context_tokens: 12000

notes:
  - "Single-RAG uses the production mixed collection and the shared experiment runner."
  - "Use the pinned Ollama bge-m3 embedding identity from the collection manifest."
  - "Canonical experiment runtime is WSL Ollama at http://localhost:11434."
