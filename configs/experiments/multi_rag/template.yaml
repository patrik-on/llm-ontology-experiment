experiment:
  name: "multi_rag_qwen25_coder_7b"
  approach: "multi_rag"
  enabled: false

model:
  config: "configs/models/qwen25_coder_7b_hf_wsl.yaml"

retrieval:
  mode: "multi_collection_rag"
  allowed_splits:
    - "train"
  indexes:
    - "test_examples"
    - "refactoring_examples"
    - "software_engineering_literature"
  fusion: "rrf"
  top_k: 3

notes:
  - "Template only; multi-collection fusion and routing are explicitly not implemented in phase 1."
