# scripts/data

CLI skripty pre dataset pipeline.

- `inspect_*.py`: rýchla kontrola štruktúry raw datasetov,
- `prepare_*.py`: príprava konkrétnych datasetov,
- `prepare_final_datasets.py`: finálne B2-R a B1 mixy.

Hlavná opakovateľná logika je v `src/llm_ontology/data/`.
