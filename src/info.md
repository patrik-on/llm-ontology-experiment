# src

Zdrojový Python kód projektu.

Hlavný balík je `src/llm_ontology/`. Skripty v `scripts/` používajú tento balík pre dataset loading, prompt formatting, model loading, inferenciu a výpočet metrík.
